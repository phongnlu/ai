version: 1.0
runtime: nodejs22

build:
  commands:
    pre-build:
      - npm install
    build:
      - npm run build

run:
  command: npm start
  network:
    port: 3000
  env:
    - name: NODE_ENV
      value: production
    - name: NEXT_TELEMETRY_DISABLED
      value: "1"
